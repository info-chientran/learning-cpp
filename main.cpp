#include <iostream>
using namespace std;

/*
 * [Lesson-20] Valid Parentheses
 *
 * _____EN_____
 * Given a string s containing just the characters '(', ')', '{', '}', '[' and ']', determine if the input string is valid.
 * An input string is valid if:
 * 1. Open brackets must be closed by the same type of brackets
 * 2. Open brackets must be closed in the correct order
 * 3. Every close bracket has a corresponding open bracket of the same type
 *
 * Constraints:
 * 1. 1 <= s.length <= 104
 * 2. s consists of parentheses only '()[]{}'.
 *
 *
 * _____VN_____
 * Cho một chuỗi s trong đó chứa các kí tự (, ), {, }, [ và ]. Hãy kiểm tra xem chuỗi s có phải chuỗi hợp lệ với các tiêu chí sau:
 * 1. Dấu ngoặc mở phải được đóng bằng dấu ngoặc cùng loại.
 * 2. Dấu ngoặc mở phải được đóng theo đúng thứ tự.
 * 3. Mỗi dấu ngoặc đóng có một dấu ngoặc mở tương ứng cùng loại.
 *
 * Điều kiện
 * 1. 1 <= s.length
 *
 */

class Solution {
public:
    bool isValid(string s) {}
};